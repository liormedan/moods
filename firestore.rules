rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all users under any document
    // This is for development/emulator use only
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
